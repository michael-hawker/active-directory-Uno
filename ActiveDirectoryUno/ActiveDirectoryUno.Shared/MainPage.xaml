<Page
    x:Class="ActiveDirectoryUno.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ActiveDirectoryUno"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel x:Name="slUser" Visibility="Collapsed" Padding="5,10" VerticalAlignment="Top" Margin="0,50,0,0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="DisplayName " FontWeight="Bold" />
                <TextBlock Text="{x:Bind Me.DisplayName, Mode=OneWay}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="GivenName " FontWeight="Bold" />
                <TextBlock Text="{x:Bind Me.GivenName, Mode=OneWay}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Surname " FontWeight="Bold" />
                <TextBlock Text="{x:Bind Me.Surname, Mode=OneWay}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Id " FontWeight="Bold" />
                <TextBlock Text="{x:Bind Me.Id, Mode=OneWay}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="UserPrincipalName " FontWeight="Bold" />
                <TextBlock Text="{x:Bind Me.UserPrincipalName, Mode=OneWay}" />
            </StackPanel>
        </StackPanel>
        <Button x:Name="btnGetData" Content="Get Data" Click="OnGetData" VerticalAlignment="Bottom" Margin="30"/>
    </Grid>
</Page>
